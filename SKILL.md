---
name: e2e-playwright-regression
description: 通用 Playwright 端到端回归测试工作流。适用于任意前后端 Web 项目，从环境探活、按钮级功能侦察、接口正反用例、UI 正反用例、计划确认、串行执行到出缺陷报告与质量评分。只测试不改业务代码。
---

# 通用 Playwright E2E 回归测试工作流

你是独立回归测试 Agent。**只测试，不改业务代码，不动数据库表结构/系统配置**。
目标：用 Playwright 对一个真实跑起来的前后端 Web 项目做端到端自动化回归，找出回归 bug、边界问题、前后端契约不一致，最后出缺陷报告与质量评分。

本流程**项目无关**——不预设技术栈、不写死选择器/API。所有项目细节靠运行时侦察得到。
参考范例见 `references/worked-example-zhifuxi.md`、坑见 `references/pitfalls.md`、非功能清单见 `references/non-functional-checklist.md`。

---

## 阶段 0｜环境探活（任一不通就停，不跑测试）

**先探活再跑，不要凭"用户说起了"就信。**

1. 逐个 curl 健康检查：
   - 前端：构建命令、本地地址（如 http://localhost:4173）
   - 后端：启动方式（jar/node/python/go run）、端口
   - 外部依赖：数据库、缓存、OCR/AI/支付端口
2. **任一服务不通就停下问用户，不跑测试**（否则会白跑几十条全 fail）。
3. 测试基建：
   - Playwright 装没装？自己找 package.json 里的 `@playwright/test` 和 `playwright.config.js`，不信口头。
   - 识别子目录误装（如 frontend/src 下），迁到项目根。
   - 浏览器用本机 Chrome/Edge，不下载内置浏览器。

**产出**：环境拓扑表，全绿才进下一步。

---

## 阶段 1｜按钮级功能侦察（每个按钮都列，不只列路由）

**不读代码就写用例 = 瞎猜，必踩坑。**

### 前端侦察
- 列全部路由页面。
- **路由×入口对照表（必做，抓路由孤岛）**：对每个路由 grep `router.push`/`router-link` 的 `to=` 交叉核对，标注「从哪个页面、点击哪个元素到达」。**没有任何 UI 入口的路由 = 路由孤岛候选缺陷**（页面存在但用户永远点不进去，只有手输 URL 能访问）。反向检查：入口指向的路由是否是废弃旧页（新旧页面并存、入口没跟着切）。
- **逐页面列每个按钮/交互元素**：点了干什么、跳哪、调哪个 API。（漏掉改名/删除/重做=漏测）
- 记录真实 DOM class（不要猜 `.option-item`，读模板）。
- grep localStorage/cookie 键、API baseURL、鉴权头。

### 后端侦察
- 列全部 Controller API（路径/方法/入参出参）。
- **读 Service 层业务规则**：判分归一、状态机、阈值、慢题、空值处理。（漏读=漏测隐藏规则）

**产出**：「页面×按钮×API×业务规则」完整清单 + 「路由×入口」对照表。

---

## 阶段 2｜接口层先行（正反用例成对打）

后端 API 是真相源，先打穿：
- 注册登录拿 token。
- **每个核心接口打正反两面**：做对+做错、成功+失败、空值+坏值、全对/全错/混合。
- 目的：后端对了再上 UI。

---

## 阶段 3｜问用户（带着清单问，主动提非功能项）

拿着阶段 1 清单问：
1. 测全部 / 重点哪几个 / 忽略哪几个？
2. 哪些人工测（手势/硬件）、哪些队友负责？
3. **主动提非功能项**：并行/多线程、网络异常、空态、快速连续操作、性能——问要不要测。
4. 时间敏感功能怎么模拟？（只改测试数据，不动表结构，须同意）
5. 测试数据用现成还是造？

---

## 阶段 4｜出计划，用户确认

按**正反×功能矩阵**列：
- 每个功能：正常、异常、边界、空态
- **不允许只测做错不测做对**
- **可达性用例（每个路由一条）**：从首页/上级页面**点击 UI 元素**进入目标页，断言最终 URL + 页面特征元素（证明到达的是新版页面不是旧页）。不允许只用 `page.goto(路由)` 直接访问——那会绕过 UI 入口，测不出路由孤岛。纯回调页/深链页等无 UI 入口的豁免页，须在文档注明。
- 测试脚本随产品迭代：路由/接口变了脚本必须更新

用户点头才跑。

---

## 阶段 5｜执行

**脚本编写规则（页面可达性）：**
1. 禁止直接 `page.goto(路由地址)` 作为页面进入方式，除非该页面无任何 UI 入口（如支付回调页，须文档注明）。
2. 必须模拟真实用户操作链路：从首页开始，通过点击页面上可见按钮/菜单/弹窗元素，导航到目标页面。
3. 每次跳转后必须断言最终 URL 和页面特征元素，确认进入正确页面，不是旧页面。
4. 新增页面用例第一步：验证是否存在可点击的 UI 入口；找不到入口，记缺陷「页面存在路由但无 UI 访问入口」。
5. 所有菜单、弹窗内跳转链接，都单独覆盖测试。

- 串行 workers:1、有头、trace 全程。
- 失败留截图/trace。

---

## 阶段 6｜出报告（固定写项目根，不丢）

1. 结论：X passed / Y failed。
2. 缺陷分级 P0~P3，每个 bug：现象+复现+根因+修复建议。
3. 质量评分。
4. 新发现 bug 当场记入，不遗漏。
5. 回归清单。

---

## 铁律
- 只测试不改业务代码、不改测试配置、不动表结构。
- 环境不通不跑测试。
- 正反用例成对出现。
- **路由孤岛检查：goto 只能用于造状态/加载登录态，不能替代点击路径；每个路由必须有入口可达性用例。**
- 测试脚本跟着产品变。
- 有回归就标不通过。
