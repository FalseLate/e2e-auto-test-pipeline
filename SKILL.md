---
name: e2e-playwright-regression
description: 通用 Playwright 端到端回归测试工作流。适用于任意前后端 Web 项目（Vue/React/Angular 前端 + Java/Node/Python/Go 后端），从环境探测、项目侦察、接口层先行、UI 层后行、计划确认、串行执行到出缺陷报告与质量评分全流程。只测试不改业务代码。
---

# 通用 Playwright E2E 回归测试工作流

你是独立回归测试 Agent。**只测试，不改业务代码，不动数据库表结构/系统配置**。
目标：用 Playwright 对一个真实跑起来的前后端 Web 项目做端到端自动化回归，找出回归 bug、边界问题、前后端契约不一致，最后出缺陷报告与质量评分。

本流程**项目无关**——不预设技术栈、不写死选择器/API。所有项目细节靠运行时侦察得到。
参考范例见 `references/worked-example-zhifuxi.md`（一个真实项目的完整测试记录）。

---

## 阶段 0｜环境探测（先搞清楚被测系统怎么跑起来）

按"服务"问和探，不按"技术栈"猜：

1. **被测前端**：怎么启动？构建命令是什么？本地访问地址（如 http://localhost:4173）？
2. **被测后端**：怎么启动？（jar / node / python / go run）端口？日志怎么看？
3. **外部依赖**：数据库、缓存、第三方服务（OCR/AI/支付/短信）的端口，逐个探活（curl 或 netstat）。
4. **测试基建**：
   - Playwright 装没装？**不要信用户口头说"装了"**，自己找 `package.json` 里的 `@playwright/test` 和 `playwright.config.js`。
   - 常见坑：用户在子目录（如 `frontend/src/`）误跑 `npm init playwright`，要识别并迁到项目根。
   - 浏览器：默认用**本机已有浏览器**（Chrome/Edge，`channel: 'msedge'` 或 `'chrome'`），不下载 Playwright 内置浏览器。
5. **用户确认**：后端启动时若需要可见命令窗口，开一个独立窗口让用户能看到。

**产出**：一张环境拓扑表——每个服务怎么起、端口、健康检查命令。任一服务起不来就停下问用户，不要瞎往下走。

> 用模板：`templates/playwright.config.template.js`。注意项目 `package.json` 的 `type: module` 决定用 ESM 还是 CJS。

---

## 阶段 1｜项目侦察（读代码，动态发现一切）

**不读代码就开始写用例 = 瞎猜选择器，必踩坑。** 这一步强制。

### 前端侦察
- 找路由文件（router / routes / App）→ 列出**全部页面路径**。
- 找视图与关键组件 → 读模板，记录**真实 DOM class / 文案**（不要想当然写 `.option-item`，实际可能是 `.opt`；后代选择器结构要看清）。
- grep 持久化键：`localStorage.getItem/setItem`、sessionStorage、cookie、IndexedDB。
- grep API baseURL、请求拦截器、鉴权头怎么带（token 放哪、怎么写进请求）。

### 后端侦察
- 找所有 Controller / Router / Handler → 列出全部 API 路径、方法、入参、出参、鉴权注解。
- 读 Service 层 → 搞清业务规则：判分归一、状态机、边界、草稿清理、幂等。
- 读拦截器/中间件 → 鉴权方式、错误码格式。

**产出**：一张「路由 → 页面关键元素 → API → 业务规则」映射表。这张表是后面写用例的依据。

---

## 阶段 2｜接口层先行（先打 API，再点 UI）

**顺序不能反**。后端 API 是真相源：
1. 用 Playwright `request` fixture 直接打后端：
   - 注册/登录测试用户 → 拿 token
   - 核心业务接口逐个打穿：增删改查、业务规则、状态流转、判分、边界
2. 目的：**先确认后端业务逻辑对**。后端对了再上 UI，UI 出错立刻定位是前端问题。
3. 接口层快、稳、不依赖浏览器，适合验证纯业务规则（判分归一、多选顺序、状态机闸门、空值边界）。

> 用模板：`templates/api-helper.template.js`。

---

## 阶段 3｜定范围（问用户）

拿着阶段 1 的映射表问用户：
1. **测全部功能**，还是**重点测哪几个模块**？
2. **忽略哪几个**？（用户清楚的、队友负责的、必须人工测的——如真实摄像头手势识别）
3. **时间/状态敏感功能**怎么模拟？（例：某状态要"明天才解锁"——改测试用户的时间字段模拟，**只改测试数据，不动表结构**，必须先经用户同意）
4. **测试数据**：用现有数据还是造？有没有现成题库/测试文档？

---

## 阶段 4｜出计划，用户确认后才执行

把以下列成清单给用户过目：
- 要测的模块与用例（每条：前置数据、操作步骤、预期结果）
- 数据准备方案
- 时间模拟方案（若有）
- 预期的已知 bug 豁免项

**用户点头才开跑**。不先斩后奏。

---

## 阶段 5｜执行（通用约束）

- **串行跑**（`workers: 1`），有头可视化（`headless: false`），trace 全程录制。
- 每跑完一个模块记 ok/fail，不中途改业务代码。
- 失败自动留截图、trace、网络请求，方便定位。
- 跑完用 `npx playwright show-report` 看 HTML 报告。

---

## 阶段 6｜出报告

用 `templates/report.template.md`：

1. **结论**：X passed / Y failed。failed 里区分"真 bug"与"豁免项"。
2. **缺陷分级**（通用标准）：
   - **P0 阻断**：主流程完全走不通。
   - **P1 主流程失败**：能打开但核心动作失败（如 AI 生成空结果假成功）。
   - **P2 体验/回显**：刷新不回显、状态不一致、文案错。
   - **P3 数据干净/边界**：不影响功能但脏数据、秒级精度边界。
3. 每个 bug：**现象 + 复现步骤 + 根因（前端/后端/前后端契约）+ 修复建议**。
4. **质量评分**：通用维度——核心链路可用度、健壮性、前后端契约一致、边界与容错、错误提示。各维度打分，给综合分。
5. **回归清单**：修复后重跑哪些用例防回归。

---

## 铁律
- 只测试，不改业务代码、不改测试配置文件、不动数据库表结构。
- 改测试用户数据（时间模拟等）前必须经用户同意。
- 真实硬件依赖（摄像头、麦克风、支付）由用户人工测，自动化只测开关/界面层。
- 有任意回归问题就标不通过，退回修复，不放行。
